name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: ./configure
      working-directory: c++
    - name: make
      run: make
      working-directory: c++
    - name: make check
      run: make check
      working-directory: c++
    - name: make distcheck
      run: make distcheck
      working-directory: c++
